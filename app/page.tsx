export default function HomePage() {
  return (
    <main className="welcome">
      <section className="welcome__card" aria-labelledby="welcome-title">
        <p className="welcome__eyebrow">Gestor de tarefas</p>
        <h1 id="welcome-title">A operação do dia, em um só lugar.</h1>
        <p>O ambiente inicial está pronto para receber os fluxos da equipe.</p>
      </section>
    </main>
  );
}
